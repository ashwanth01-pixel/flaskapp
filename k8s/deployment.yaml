# Updated by bot: Add readiness and liveness probes to ensure proper health checking of the application
# Updated by bot: Add resource limits and requests to the container spec to ensure proper resource allocation
# Updated by bot: applied fix
Add a rolling update strategy to the deployment spec: strategy: {type: RollingUpdate, rollingUpdate: {maxSurge: 1, maxUnavailable: 0}}
# Updated by bot: applied fix
Add resource limits and requests to the container spec to ensure proper resource allocation
# Updated by bot: applied fix
Add a rolling update strategy to the deployment spec: strategy: {type: RollingUpdate, rollingUpdate: {maxSurge: 1, maxUnavailable: 0}}
# Updated by bot: applied fix
Add resource limits and requests to the container spec to ensure proper resource allocation and prevent resource exhaustion
# Updated by bot: applied fix
Add a rolling update strategy to the deployment spec: strategy: {type: RollingUpdate, rollingUpdate: {maxSurge: 1, maxUnavailable: 0}}
# Updated by bot: applied fix
Add resource limits and requests to the container spec to ensure proper resource allocation and prevent resource exhaustion
# Updated by bot: applied fix
Add a rolling update strategy to the deployment spec: strategy: {type: RollingUpdate, rollingUpdate: {maxSurge: 1, maxUnavailable: 0}}
# Updated by bot: applied fix
Add resource limits and requests to the container spec to ensure proper resource allocation and prevent resource exhaustion
# Updated by bot: applied fix
Add a rolling update strategy to the deployment spec: strategy: {type: RollingUpdate, rollingUpdate: {maxSurge: 1, maxUnavailable: 0}}
# Updated by bot: applied fix
Add resource limits and requests to the container spec to ensure proper resource allocation
# Updated by bot: applied fix
Add a rolling update strategy to the deployment spec: strategy: {type: RollingUpdate, rollingUpdate: {maxSurge: 1, maxUnavailable: 0}}
# Updated by bot: applied fix
Add resource limits and requests to the container spec to ensure proper resource allocation
# Updated by bot: applied fix
Add a rolling update strategy to the deployment spec: strategy: {type: RollingUpdate, rollingUpdate: {maxSurge: 1, maxUnavailable: 0}}
# Updated by bot: applied fix
Add resource limits and requests to the container spec to ensure proper resource allocation and prevent resource exhaustion.
# Updated by bot: applied fix
Add a rolling update strategy to the deployment spec: strategy: {type: RollingUpdate, rollingUpdate: {maxSurge: 1, maxUnavailable: 0}}
# Updated by bot: applied fix
Add resource limits and requests to the container spec to ensure proper resource allocation and prevent resource exhaustion.
# Updated by bot: Add a rolling update strategy to the deployment spec: strategy: {type: RollingUpdate, rollingUpdate: {maxSurge: 1, maxUnavailable: 0}}
# Updated by bot: Add resource limits and requests to the container spec to ensure proper resource allocation and prevent resource exhaustion.
# Updated by bot: Add a rolling update strategy to the deployment spec: strategy: {type: RollingUpdate, rollingUpdate: {maxSurge: 1, maxUnavailable: 0}}
# Updated by bot: Add resource limits and requests to the container spec to ensure proper resource allocation and prevent resource exhaustion.
# Updated by bot: Add a rolling update strategy to the deployment spec: strategy: {type: RollingUpdate, rollingUpdate: {maxSurge: 1, maxUnavailable: 0}}
# Updated by bot: Add resource limits and requests to the container spec to ensure proper resource allocation and prevent resource exhaustion.
# Updated by bot: Add a rolling update strategy to the deployment spec: strategy: {type: RollingUpdate, rollingUpdate: {maxSurge: 1, maxUnavailable: 0}}
# Updated by bot: Add a rolling update strategy to the deployment spec: strategy: {type: RollingUpdate, rollingUpdate: {maxSurge: 1, maxUnavailable: 0}}
# Updated by bot: Add a rolling update strategy to the deployment spec: strategy: {type: RollingUpdate, rollingUpdate: {maxSurge: 1, maxUnavailable: 0}}
# Updated by bot: Add a rolling update strategy to the deployment spec: strategy: {type: RollingUpdate, rollingUpdate: {maxSurge: 1, maxUnavailable: 0}}
# Updated by bot: Add a rolling update strategy to the deployment spec, e.g., strategy: {type: RollingUpdate, rollingUpdate: {maxSurge: 1, maxUnavailable: 0}}
# Updated by bot: Add a rolling update strategy to the deployment spec, e.g., strategy: {type: RollingUpdate, rollingUpdate: {maxSurge: 1, maxUnavailable: 0}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ashapp-backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ashapp-backend
  template:
    metadata:
      labels:
        app: ashapp-backend
    spec:
      containers:
      resources:
        requests:
          cpu: '100m'
          memory: '128Mi'
        limits:
          cpu: '500m'
          memory: '512Mi'
      resources:
        requests:
          cpu: '100m'
          memory: '128Mi'
        limits:
          cpu: '500m'
          memory: '512Mi'
      resources:
        requests:
          cpu: '100m'
          memory: '128Mi'
        limits:
          cpu: '500m'
          memory: '512Mi'
      resources:
        requests:
          cpu: '100m'
          memory: '128Mi'
        limits:
          cpu: '500m'
          memory: '512Mi'
      - name: ashapp-backend
        image: ashwanth01/ashapp-backend:latest
        ports:
        - containerPort: 5000
        resources:
          limits:
            cpu: "500m"
            memory: "256Mi"
          requests:
            cpu: "250m"
            memory: "128Mi"
        readinessProbe:
          httpGet:
            path: /
            port: 5000
          initialDelaySeconds: 10
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /
            port: 5000
          initialDelaySeconds: 15
          periodSeconds: 10
        imagePullPolicy: Always
        env:
        - name: FLASK_ENV
          value: "production"
        - name: PYTHONUNBUFFERED
          value: "1"

# Appended by bot: Add a rolling update strategy to the deployment spec, e.g., strategy: {type: RollingUpdate, rollingUpdate: {maxSurge: 1, maxUnavailable: 0}}

# Appended by bot: Add a rolling update strategy to the deployment spec, e.g., strategy: {type: RollingUpdate, rollingUpdate: {maxSurge: 1, maxUnavailable: 0}}

# Appended by bot: Add a rolling update strategy to the deployment spec, e.g., strategy: {type: RollingUpdate, rollingUpdate: {maxSurge: 1, maxUnavailable: 0}}
# Appended by bot: Add a rolling update strategy to the deployment spec: strategy: {type: RollingUpdate, rollingUpdate: {maxSurge: 1, maxUnavailable: 0}}

# Appended by bot: Add resource limits and requests to the container spec to ensure proper resource allocation

# Appended by bot: Add readiness and liveness probes to ensure proper health checking of the application